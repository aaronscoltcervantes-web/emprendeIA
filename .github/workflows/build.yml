steps:
  - uses: actions/checkout@v4
  
  - name: Set up Python
    uses: actions/setup-python@v5
    with:
      python-version: '3.11'

  - name: Build with Buildozer
    uses: kivy/buildozer-action@v1
    with:
      command: build android debug
